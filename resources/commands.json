{
    "commands":{
        "compile":{
            "w": "make",
            "l": "make",
            "d": "make",
            "docker": "docker run --rm -v $PWD:/src/durango zerasul/durangodevkit"
        },
        "clean": {
                "w": "make clean",
                "l": "make clean",
                "d": "make clean",
                "docker":"docker run --rm -v $PWD:/src/durango zerasul/durangodevkit make clean"
        }
    }
}